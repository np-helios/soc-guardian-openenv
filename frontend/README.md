# SOC Guardian Frontend

This frontend is a standard React + Vite dashboard for the SOC Guardian OpenEnv backend.

## Run locally

```bash
npm install
npm run dev
```

The dev server runs on `http://127.0.0.1:8090` and proxies `/api/*` requests to the FastAPI backend on `http://127.0.0.1:8000`.
